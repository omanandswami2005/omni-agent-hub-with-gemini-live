/**
 * Firebase SDK initialization.
 */

// TODO: Initialize Firebase with config from env:
//   import { initializeApp } from 'firebase/app';
//   import { getAuth, GoogleAuthProvider } from 'firebase/auth';
//   import { getFirestore } from 'firebase/firestore';
//
// Config from: VITE_FIREBASE_API_KEY, VITE_FIREBASE_AUTH_DOMAIN,
//   VITE_FIREBASE_PROJECT_ID, VITE_FIREBASE_STORAGE_BUCKET,
//   VITE_FIREBASE_MESSAGING_SENDER_ID, VITE_FIREBASE_APP_ID

export const app = null; // initializeApp(firebaseConfig)
export const auth = null; // getAuth(app)
export const db = null; // getFirestore(app)
export const googleProvider = null; // new GoogleAuthProvider()
