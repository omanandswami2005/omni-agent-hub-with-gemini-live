/**
 * Chat: Waveform — Audio waveform visualization.
 */

// TODO: Implement:
//   - Canvas-based waveform rendering
//   - Frequency data from AnalyserNode
//   - Smooth animation loop

export default function Waveform({ analyser, color = 'var(--primary)' }) {
  return <canvas className="h-16 w-full" />;
}
