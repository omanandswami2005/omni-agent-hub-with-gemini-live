/**
 * Layout: MobileNav — Bottom tab navigation for mobile/PWA.
 */

export default function MobileNav() {
  return (
    <nav className="fixed bottom-0 left-0 right-0 flex items-center justify-around border-t border-border bg-surface p-2 md:hidden">
      {/* Tab icons: Home, Personas, MCP, Sessions, Settings */}
    </nav>
  );
}
