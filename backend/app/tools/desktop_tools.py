"""capture_screen(), click_at(), type_text() — Desktop computer-use tools."""

# TODO: Implement tools that send commands to desktop tray client via WS:
#   - capture_screen() → request screenshot from desktop client
#   - click_at(x, y) → mouse click at coordinates
#   - type_text(text) → keyboard input
#   - open_app(name) → launch application
