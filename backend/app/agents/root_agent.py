"""Root router agent — ADK LlmAgent that delegates to persona sub-agents."""

# TODO: Implement root agent with sub_agents=[nova, atlas, sage, spark, claire]
# Uses: run_live(), LiveRequestQueue, StreamingMode.BIDI
# Features: agent_transfer, auto transcription, proactive audio, affective dialog
