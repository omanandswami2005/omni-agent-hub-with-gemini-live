"""GCS image/file storage service."""

# TODO: Implement storage service:
#   - upload_image(data, filename) → GCS URL
#   - upload_artifact(data, session_id) → GCS URL
#   - get_signed_url(gcs_path) → temporary download URL
#   - Bucket: configured via settings.GCS_BUCKET_NAME
