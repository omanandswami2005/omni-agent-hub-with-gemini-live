"""Firestore CRUD for personas."""

# TODO: Implement persona service:
#   - list_personas(user_id) → default + user-created
#   - create_persona(user_id, config) → save to Firestore
#   - update_persona(user_id, persona_id, config)
#   - delete_persona(user_id, persona_id)
#   - get_default_personas() → Nova, Atlas, Sage, Spark, Claire
