"""Dynamic McpToolset instantiation — load/unload MCP plugins at runtime."""

# TODO: Implement MCP manager:
#   - Load MCP configs from Firestore (user-specific enabled MCPs)
#   - Instantiate McpToolset per MCP (stdio or StreamableHTTP)
#   - Hot-swap tools mid-session without restart
#   - E2B MCP gateway integration (100+ built-in MCPs)
